part of 'package.dart';

class LoaderAddingNewImageMolecula extends StatelessWidget {
  const LoaderAddingNewImageMolecula({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
